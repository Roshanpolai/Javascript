const form = document.querySelector('bmiForm');
form.addEventListener('submit', function(e){
    e.preventDefault();

    cosnt  = parseInt(document.querySelector('#height').value)
    parseInt(document.querySelector('#weight').value)

})